{% extends "base.jinja" %}

{% block content %}
    <table id="revision_list">
        <thead>
            <tr>
                <td>{% trans %} Revision {% endtrans %}</td>
                <td>{% trans %} Diff {% endtrans %}</td>
                <td>{% trans %} Author {% endtrans %}</td>
                <td>{% trans %} Revert to {% endtrans %}</td>
            </tr>
        </thead>
        <tbody>
            {% for revision in revisions %}
                <tr>
                    <td>{{ revision.datetime }}</td>
                    <td><input type="checkbox" name="revision"></td>
                    <td><a href="{{ url_for('wikiShowUser', ruser.id)}}">{{ ruser.full_name }}</td>
                    <td>{% if not loop.first %}
                            <a href='{{ url_for("wikiRevertPageToRevision", page_title, revision.id)}}'>
                                {% trans %} Revert {% endtrans %}</a>
                        {% endif %}
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock content %}
